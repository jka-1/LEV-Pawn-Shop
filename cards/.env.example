# Backend configuration (example values; copy to cards/.env and fill in)

# Gemini / Google Generative Language API key
# Obtain from Google AI Studio; keep this secret and DO NOT commit.
GOOGLE_API_KEY=
# Optional: lock to a specific Gemini model supported by your key
# e.g., gemini-2.0-flash (default), gemini-2.0-flash-lite, gemini-2.5-flash-lite
GEMINI_MODEL=gemini-2.0-flash

# Application base URL for CORS (dev defaults)
APP_BASE_URL=http://localhost:5173
PORT=5000

# MongoDB (Atlas recommended)
MONGODB_URI=
MONGODB_DB=SafePawnShop
MONGODB_USERS_COLL=storefront_users
MONGODB_URLS_COLL=storefront_urls

# Auth (generate strong secrets for production)
JWT_ACCESS_SECRET=change-me
JWT_REFRESH_SECRET=change-me
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=30d

# Cookies (dev-safe defaults)
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# Email (optional; only needed if enabling verification emails)
RESEND_API_KEY=
EMAIL_FROM=no-reply@example.com
VERIFY_TOKEN_TTL_HOURS=24

# Development switch: skip email verification flow when false
REQUIRE_EMAIL_VERIFICATION=false